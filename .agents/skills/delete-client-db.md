# delete-client-db

Delete the local JVM database for Chef Mate.

## Instructions

Run the following command to delete the Chef Mate JVM database:

```sh
rm -f "/Users/asteinmetz/Library/Application Support/Chef Mate/chefmate.db"
```

After running the command, confirm to the user whether the database was successfully deleted or if it didn't exist.
